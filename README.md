# Freyr AI Web

A dependency-free static website built with HTML, CSS, and browser JavaScript.

## Run locally

Open `index.html` directly in any modern browser. No installation, build step,
or local server is required.

## Files

- `index.html` — page structure and metadata
- `styles.css` — responsive visual design
- `script.js` — optional browser-only behavior
